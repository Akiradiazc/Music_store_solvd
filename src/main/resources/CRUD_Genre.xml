<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DAO">
  <!--CREATE-->

  <!--READ-->
  <select id="SelectAllGenre" parameterType="Genre" resultType="list">
    SELECT * from Genre WHERE Genre.id = #{Genre.id}
  </select>
  <select id="SelectGenreByID" resultType="Genre">
    SELECT * from Genre WHERE Genre.id = #{Genre.id}
  </select>
  <!--UPDATE-->
  <!--DELETE-->
  <delete id="DeleteGenre" parameterType="Genre">
    DELETE FROM Genre WHERE idGenre = # {Genre.id}
  </delete>



</mapper>